<!DOCTYPE>
<html>
    <head>
        <title>date</title>
    </head>
    <body>
        <h1>Today</h1>
            <% date = DateTime.now %>
                <p><%= date.strftime("The date is %A, %B %d, %Y") %></p>
    </body>
</html>